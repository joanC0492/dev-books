---
import { SCORE_API_ENDPOINT } from "astro:env/server";

const res = await fetch(SCORE_API_ENDPOINT);
const scoreValue = await res.text();

interface Props {
  id: string;
}
const { id } = Astro.props;
---

<span class="text-xs" id={id}>
  ⭐ Puntuación: {scoreValue.trim()}/5
</span>
